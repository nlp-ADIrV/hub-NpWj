### 1. NVIDIA Nemotron 3 Super：Mamba-Transformer 混合 MoE 架构

**核心定位**：面向多智能体（Agent）场景优化的 120B 总参数 / 12B 激活参数开源模型，原生支持 1M token 上下文，主打高吞吐量与长上下文推理效率。
**整体架构**：88 层混合交错结构，由**Mamba-2 层、Transformer 注意力层、LatentMoE 层**三类模块周期性排列组成，而非纯 Transformer 或纯 SSM 架构。
**核心结构创新**：

- **分层混合设计**：主体以 Mamba-2 状态空间层为主（提供 O (n) 线性复杂度，支撑长上下文），每隔若干层插入一层 GQA 标准注意力作为 “全局锚点”（补全精确信息检索与全局依赖建模能力），兼顾效率与效果。
- **LatentMoE 潜在专家机制**：不同于常规 MoE 直接在隐藏维度路由，该架构先将 token 投影到低维潜在空间再进行专家路由与计算，以单个专家的计算成本实现等效 4 个专家的表达能力，进一步提升参数效率。
- **MTP 多令牌预测**：原生集成共享权重的多 token 预测头，推理时可作为内置推测解码引擎，无需额外草稿模型即可提升生成速度 3 倍以上。
**技术优势**：相比同规模纯 Transformer MoE 模型，长上下文场景吞吐量提升 5 倍，显存占用降低 4 倍，专门适配 Agent 长对话、工具调用、代码库分析等场景NVIDIA。

### 2. 蚂蚁 Ling-2.5 系列：混合线性注意力万亿参数模型

**核心定位**：全球首个万亿参数规模的混合线性注意力开源模型，分为 Ling-2.5-1T（即时生成模型）和 Ring-2.5-1T（深度思考模型），主打长序列高吞吐量。
**整体架构**：Decoder-only 架构，核心替换传统注意力为**1:7 比例的 MLA+Lightning Linear 混合线性注意力**，配合 MoE 专家层扩展参数规模。
**核心结构创新**：

- **高低配比混合线性架构**：7/8 的层使用 Lightning Linear Attention（纯线性复杂度，无 KV 缓存膨胀），1/8 的层保留 MLA 多头潜在注意力（保障全局语义与检索精度），既大幅降低访存，又避免纯线性注意力的长程召回短板。
- **注意力残差技术**：针对万亿参数深层模型信号衰减问题，优化了跨层注意力信息的传递与复用路径，让超大规模线性注意力模型能稳定训练收敛。
- **KV 缓存极致压缩**：通过 QK 归一化、部分 RoPE 位置编码等优化，配合线性注意力的固定状态大小，将长文本访存规模压缩至传统架构的 1/10。
**技术优势**：32K 以上长序列场景生成吞吐量提升 3 倍以上，序列越长优势越明显，同时在推理、代码等任务上保持与传统注意力万亿模型相当的精度ModelScope。

### 3. OpenMythos：循环深度 Transformer（RDT）

**核心定位**：基于 Claude Mythos 架构原理复现的开源实现，探索 “权重循环复用” 的新缩放范式，而非传统的堆层堆参数路线。
**整体架构**：三段式结构 —— **Prelude 前置浅层 Transformer + Recurrent Block 循环核心块 + Coda 终章输出层**，核心计算块权重共享，通过循环执行次数动态控制推理深度。
**核心结构创新**：

- **循环深度替代堆叠深度**：传统模型靠堆叠不同权重的层增加深度，该架构仅用一个共享权重的 Transformer 块，在前向传播中循环执行 N 次（最多 16 次），每次更新隐藏状态，等效于深度增加但参数量不变。
- **潜空间静默思考**：循环过程全部在隐藏状态空间完成，不需要像思维链 (CoT) 那样输出中间 token，即可实现多步推理，推理深度可按需动态调节。
- **MoE + 循环条件计算**：循环块内集成 MoE 路由，不同循环步可激活不同专家，实现 “思考步数越多，激活参数越充分” 的条件计算模式。
**技术优势**：相同参数量下推理能力显著提升，推理成本与深度成正比而非与总参数成正比；可灵活在 “快速回答” 和 “深度思考” 模式间切换，无需切换模型36氪。

### 4. Aether-7B-5Attn：异质注意力混合实验模型

**核心定位**：完全开源（含数据、代码、训练全流程）的实验性基座模型，核心命题是验证 “不同层使用不同注意力机制” 是否比全层统一注意力更高效。
**整体架构**：49 层 Decoder-only MoE 模型（总 6.59B 参数，单 token 激活～2.98B），5 种注意力机制按**7×7 拉丁方阵**均匀分布在各层，避免单一机制的缺陷被重复放大。
**核心结构创新**：

- **五种异构注意力协同**：包含全注意力 (Full)、滑动窗口 (Sliding)、差分注意力 (Differential)、原生稀疏注意力 (NSA)、混合注意力 (Hybrid) 五种，分别擅长全局建模、局部模式、降噪、长程稀疏关联、综合能力。
- **拉丁方均匀排布**：49 层按 7×7 拉丁方排列，每种注意力各占 7 层，且在不同网络深度均匀分布，让低层到高层都能获得对应归纳偏置。
- **全透明科研设计**：完整公开训练数据配方、代码、日志、检查点，是少有的可复现的异构架构对照实验模型。
**技术优势**：在相同计算量下，综合性能优于单一全注意力基线；不同注意力机制互补，在局部语义、全局推理、长文本等不同任务上均有稳定表现。

### 5. DeepSeek Engram：条件记忆 + 神经计算双稀疏架构

**核心定位**：探索 “静态记忆” 新维度的开源架构，与 MoE 的 “计算稀疏” 形成互补，目标是用更低计算成本实现更强语言建模能力。
**整体架构**：在标准 Transformer/MoE 基础上，注入**大规模静态 N-gram 嵌入表**作为 Engram 记忆模块，每个 token 通过哈希查表快速获取常识性语言模式。
**核心结构创新**：

- **双稀疏范式**：MoE 是 “计算稀疏”（动态选专家），Engram 是 “记忆稀疏”（静态查表）。常见的语言模式（如固定搭配、常识短语）直接从静态表中读取，罕见的复杂语义交给神经网络处理，两者互补。
- **哈希 O (1) 查表**：对每个 token 位置，通过 2-3 元组哈希直接查找嵌入表，计算开销可忽略，不增加神经网络深度。
- **最优配比缩放定律**：实证得出 “75-80% 神经计算 + 20-25% 静态记忆” 为最优配比，同等 FLOPs 下，在 MMLU、BBH 等基准上显著优于纯 MoE 基线。
**技术优势**：几乎不增加推理计算量的前提下提升模型能力，尤其适合低算力部署场景；静态记忆可单独更新，无需重新训练整个模型。

### 6. RWKV-7：动态演化 RNN 架构（纯无注意力）

**核心定位**：纯循环神经网络路线的旗舰开源模型，完全摒弃自注意力机制，实现真正的 O (n) 时间与 O (1) 推理内存复杂度。
**整体架构**：基于加权键值状态 (WKV) 的 RNN 架构，无 Transformer 注意力模块，训练时可并行化，推理时按 RNN 串行执行。
**核心结构创新**：

- **广义 Delta Rule 更新机制**：引入向量化学习率和向量化衰减因子，支持按通道独立更新内部状态，突破传统 RNN 单一刻度的限制，记忆管理更精细。
- **替换键与移除键分离**：状态更新时写入和擦除使用不同的键向量，可更灵活地编辑记忆内容，类似人脑的记忆更新机制，缓解长序列信息干扰。
- **动态状态演化**：内部状态随输入持续演化，天然支持无限长上下文；推理时只需保存固定大小的状态向量，无需随序列长度增长的 KV 缓存。
**技术优势**：训练推理成本比同尺寸 Transformer 低 1-2 个数量级，端侧部署友好；长文本生成内存恒定，无长度爆炸问题。