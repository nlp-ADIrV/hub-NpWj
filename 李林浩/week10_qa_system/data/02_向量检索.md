# 向量检索与关键词检索

关键词检索主要根据词语是否出现以及词频进行匹配。BM25 是经典的关键词检索算法，它综合考虑词频、文档长度和词语在整个文档集合中的稀有程度。

向量检索会将文本转换为向量，然后计算问题向量和文档向量之间的相似度。它的优势是能够处理一定程度的语义相似，例如问题和原文使用不同表达时仍然可能被检索到。

向量数据库主要用于存储向量、建立近似最近邻索引并执行相似度查询。常见的索引方法包括 HNSW、IVF 等。

混合检索会同时使用关键词检索和向量检索，再对两类结果进行融合。关键词检索适合精确术语和编号查询，向量检索适合语义表达，因此混合检索在实际系统中往往更稳定。
